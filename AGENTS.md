# AI 协作规则

本文件是本项目的 AI 执行规则。每次在本仓库中执行任务前，应先阅读本文件，并结合 `PROJECT_STRUCTURE.md` 理解项目现状。

## 沟通规则

- 默认使用中文和用户沟通。
- 修改完成后，简要说明改了哪些文件、为什么这么改、是否已验证。
- 遇到不确定的业务规则、数据来源或交互逻辑时，先向用户确认。
- 不要用猜测代替检查。开始编码前，先阅读相关页面、组件、数据文件和配置。

## 项目背景

- 这是一个微信小程序项目，定位是“礼金、礼物、请客往来记录本”。
- 当前主要使用原生小程序技术：`Page`、`Component`、WXML、WXSS、本地 JS 数据层和微信本地缓存。
- 当前没有 npm 依赖、后端接口或云开发接入。
- 主要通过微信开发者工具打开和调试项目。

## 文件和结构约定

- 项目结构说明优先查看 `PROJECT_STRUCTURE.md`。
- 页面代码放在 `pages/`。
- 公共组件放在 `components/`。
- 本地 JS 数据层放在 `data/`。
- 全局配置主要在 `app.json`，项目配置主要在 `project.config.json`。
- 新增页面时，必须同步检查并更新 `app.json` 的 `pages` 配置。
- 每次项目结构发生变化时，必须同步更新 `PROJECT_STRUCTURE.md`，包括新增、删除、移动或重命名页面、组件、数据文件、资源目录、配置文件，以及重要职责变化。
- 不要随意移动目录、重命名文件或调整既有结构，除非用户明确要求。

## 编码规则

- 优先保持现有代码风格和小程序原生写法。
- 修改范围要尽量聚焦当前任务，避免顺手重构无关代码。
- 不要删除用户已有代码、配置或资源，除非用户明确要求。
- 不要引入大型依赖或构建流程，除非用户明确需要。
- 对 WXML、WXSS、JS、JSON 的修改要注意微信小程序语法限制。
- 自定义导航栏和自定义底部 TabBar 是当前项目的重要约定，修改页面时要保留相关组件使用方式。

## 数据和功能规则

- 当前数据主要来自 `data/records.js` 和 `data/contacts.js`。
- 涉及新增、删除、编辑、持久化、云开发、后端接口时，先确认数据源方案。
- 当前部分功能仍是原型或占位状态，例如编辑记录真实保存、删除联系人、部分我的页面业务逻辑等。新建记录已接入微信本地缓存，但还没有云端持久化。
- 修改统计、记录列表、联系人详情等逻辑时，要检查对应数据字段是否一致。

## 样式和交互规则

- 保持微信小程序界面简洁、清晰、适合移动端。
- 样式优先局部修改页面或组件 WXSS，避免不必要地影响全局 `app.wxss`。
- 新增交互时要考虑空状态、错误状态和移动端点击区域。
- 不要添加与当前产品风格不一致的大型视觉改版，除非用户明确要求。

## 验证规则

- 修改 JSON 文件后，注意检查语法合法性。
- 修改页面或组件后，检查引用路径、组件声明和事件绑定是否一致。
- 如果本次任务改变了项目结构或模块职责，回复前检查 `PROJECT_STRUCTURE.md` 是否已经同步更新。
- 如果无法在当前环境直接运行微信开发者工具，应在回复中说明未能完整运行，并说明已做的静态检查。

## 编码读取注意事项

- 项目文档可能包含中文。使用 PowerShell 读取时，优先指定 UTF-8，例如：

```powershell
Get-Content -Encoding UTF8 PROJECT_STRUCTURE.md
```

- 如果终端显示乱码，不要据此判断原文件内容错误，应换用 UTF-8 方式重新读取。
