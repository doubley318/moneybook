1. 每次从其他页面返回首页后，数据消失，需要等到服务器的数据下拉成功后才能显示；
    解决方法：
        1. 保存数据到本地，解决下一次打开小程序的时候面板数据为空的问题；
        2. 先读取本地数据（本地存储数据和内存数据），再异步拉取服务器数据，这样返回任意页面都不会出现数据为空的问题；
2. 创建记录的选择日期会偶现不能点击确定的问题；
    
3. 联系人页面没有单独请求，会造成拉取冗余数据导致的流量和负载过大；
4. 